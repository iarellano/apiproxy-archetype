# Global test profile configurations
---
apigee:
  env: test
  org: ${testOrganization}

${apiproxyName}:
  northbound:
    domain: ${testProxyDomain}
    port: ${testProxyPort}
    basePath: ${apiproxyBasePath}
    protocol: ${testProtocol}
    virtualHost: ${testProxyVirtualHost}