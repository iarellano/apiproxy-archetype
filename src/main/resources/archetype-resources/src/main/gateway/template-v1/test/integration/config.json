{
    "parameters": {
        "domain": "${northbound.domain}",
        "basepath": "${northbound.basePath}${deployment.suffix}",
        "${shared.resource.name}-test-app": {
            "name": "${shared.resource.name}-test-app",
            "clientId": "",
            "clientSecret": ""
        }
    }
}