{
    "parameters": {
        "domain": "${northbound.domain}",
        "basepath": "${northbound.basePath}${deployment.suffix}",
        "${shared.resource.name}-app": {
            "name": "${shared.resource.name}-app",
            "clientId": "",
            "clientSecret": ""
        }
    }
}