{
    "parameters": {
        "domain": "${northbound.domain}",
        "basepath": "${northbound.basePath}${deployment.suffix}",
        "${apiproxy.canonicalName}-app": {
            "name": "${apiproxy.canonicalName}-app",
            "clientId": "",
            "clientSecret": ""
        }
    }
}