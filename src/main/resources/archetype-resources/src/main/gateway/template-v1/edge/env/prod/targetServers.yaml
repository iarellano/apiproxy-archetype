---
- name: ${apiproxy.name}-rev${apiproxy.revision}${deployment.suffix}
  host: httpbin.org
  isEnabled: true
  port: 443
  sSLInfo:
    clientAuthEnabled: 'false'
    enabled: 'true'
    ignoreValidationErrors: 'true'
